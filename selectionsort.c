#include <stdio.h>
void selectionSort(int *arr, int size)
{
    for (int i = 0; i < size; i++)
    {
        int pos = i;
        int max = arr[i];
        for (int j = i; j < size; j++)
        {
        }
    }
}
